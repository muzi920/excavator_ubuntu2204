# ============================================================
# shandong_ws 项目依赖清单
# 基于系统 Python 3.10.12 + ROS2 Humble
# 生成日期: 2026-09-10
# ============================================================

# ROS2 核心依赖 (rclpy + 消息类型)
# 注意: 这些包通常通过 apt 安装，而非 pip
# 如果使用 pip 安装，需要先 source /opt/ros/humble/setup.bash
rclpy>=3.3.0
geometry-msgs>=4.9.0
sensor-msgs>=4.9.0
std-msgs>=4.9.0
std-srvs>=4.9.0
nav-msgs>=4.9.0
visualization-msgs>=4.9.0
tf2-geometry-msgs>=0.25.0
tf2-ros-py>=0.25.0
cv-bridge>=3.2.0
image-geometry>=3.2.0

# 科学计算与数据处理
numpy>=1.21.0,<2.0.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.5.0

# 计算机视觉
opencv-python>=4.5.0

# 机器学习 (可选，按需安装)
torch>=2.0.0
torchvision>=0.15.0
scikit-learn>=1.0.0

# 深度学习框架 (可选)
# transformers>=4.30.0
# accelerate>=0.20.0

# 3D 点云处理
open3d>=0.17.0

# ROS2 工具 (可选)
# colcon-common-extensions>=0.3.0
# ros2bag>=0.15.0

# GUI 相关
PyQt5>=5.15.0

# 数据序列化
PyYAML>=6.0
protobuf>=3.20.0

# 网络与通信
requests>=2.28.0
paramiko>=2.11.0

# 日志与调试
loguru>=0.7.0

# 其他工具
tqdm>=4.64.0
pillow>=9.0.0
