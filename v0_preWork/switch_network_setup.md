# 海康摄像头与 M300 雷达统一网段并接交换机操作步骤

本文档用于说明：如何将当前不在同一网段的海康威视摄像头和 `M300` 雷达，修改到同一个局域网网段，并通过交换机同时接入电脑进行访问和采集。

当前已确认设备信息：

- 海康摄像头 IP：`192.168.1.64`
- 海康摄像头 MAC：`08:3b:c1:c8:4d:40`
- M300 雷达 IP：`192.168.158.98`
- M300 雷达 MAC：`50:0c:20:58:ed:1b`

---

## 1. 目标

将两个设备统一到同一个网段，方便：

- 同时接入交换机
- 用一台电脑统一访问
- 后续写采集程序时使用固定 IP
- 避免每次切换电脑网口地址

本文推荐统一到以下网段：

- 电脑：`192.168.10.10`
- 海康摄像头：`192.168.10.101`
- M300 雷达：`192.168.10.201`
- 子网掩码：`255.255.255.0`
- 网关：如果只是本地直连交换机，可先留空

说明：

- 摄像头放在 `100` 段，便于识别
- 雷达放在 `200` 段，便于识别
- 电脑固定为 `192.168.10.10`，便于长期使用

---

## 2. 所需设备

- Ubuntu 电脑一台
- 普通交换机一台
- 海康威视摄像头一台
- `M300` 雷达一台
- 网线若干
- 摄像头和雷达各自对应的供电电源

注意事项：

- 如果交换机不是 PoE 交换机，则摄像头和雷达必须各自单独供电
- 初次配置时建议一次只接一台设备，避免 IP 冲突和排查混乱

---

## 3. 总体操作顺序

推荐按下面顺序做：

1. 单独连接海康摄像头，修改其 IP 为 `192.168.10.101`
2. 单独连接 M300 雷达，修改其 IP 为 `192.168.10.201`
3. 将电脑网口改为 `192.168.10.10/24`
4. 将摄像头和雷达一起接入交换机
5. 统一验证两个设备都能访问

不要一开始就把两个设备一起插入交换机并尝试改 IP。

原因：

- 两台设备当前不在同一网段
- 初次修改时逐台处理更安全
- 某些设备改 IP 后需要重新连接验证

---

## 4. 第一步：修改海康摄像头 IP

### 4.1 单独连接海康摄像头

先只连接：

- 电脑
- 海康摄像头

不要连接雷达。

摄像头接法：

- 摄像头接电源
- 摄像头网线接电脑，或者接交换机后再接电脑，但交换机上只保留这一台设备

### 4.2 将电脑网口切到摄像头当前网段

海康摄像头当前 IP 为 `192.168.1.64`，所以电脑网口先设为：

```bash
sudo ip addr flush dev eno1
sudo ip addr add 192.168.1.10/24 dev eno1
sudo ip link set eno1 up
ping -c 4 192.168.1.64
```

如果 `ping` 正常，说明当前连接正确。

### 4.3 登录海康摄像头管理页面

在浏览器中打开：

```text
http://192.168.1.64
```

如果 `http` 打不开，再试：

```text
https://192.168.1.64
```

登录后进入网络设置页面，修改参数为：

- IP：`192.168.10.101`
- 子网掩码：`255.255.255.0`
- 网关：留空，或填 `192.168.10.1`

建议：

- 如果后续不接路由器，仅本地交换机组网，网关可以先留空
- 如果将来接路由器，也可以统一填 `192.168.10.1`

### 4.4 修改完成后重新验证

摄像头 IP 改完后，旧地址 `192.168.1.64` 会失效。

此时要把电脑网口改到新网段：

```bash
sudo ip addr flush dev eno1
sudo ip addr add 192.168.10.10/24 dev eno1
sudo ip link set eno1 up
ping -c 4 192.168.10.101
```

如果 `ping` 通，说明海康摄像头改 IP 成功。

---

## 5. 第二步：修改 M300 雷达 IP

### 5.1 单独连接 M300 雷达

海康摄像头先断开，只保留：

- 电脑
- M300 雷达

### 5.2 将电脑网口切到雷达当前网段

M300 雷达当前 IP 为 `192.168.158.98`，电脑网口设为：

```bash
sudo ip addr flush dev eno1
sudo ip addr add 192.168.158.15/24 dev eno1
sudo ip link set eno1 up
ping -c 4 192.168.158.98
```

如果 `ping` 正常，说明电脑已经能访问雷达。

### 5.3 修改 M300 雷达 IP

这一部分取决于雷达的配置方式，常见情况有三种：

1. 雷达有网页管理页面
2. 雷达有厂商专用上位机软件
3. 雷达通过串口命令、TCP 命令或专用协议改 IP

你需要使用该雷达对应的配置方式，将其 IP 改为：

- IP：`192.168.10.201`
- 子网掩码：`255.255.255.0`
- 网关：留空，或填 `192.168.10.1`

如果你已经有厂商说明书，优先按说明书中的改 IP 步骤操作。

### 5.4 修改完成后重新验证

改完雷达 IP 后，把电脑网口切换到统一网段：

```bash
sudo ip addr flush dev eno1
sudo ip addr add 192.168.10.10/24 dev eno1
sudo ip link set eno1 up
ping -c 4 192.168.10.201
```

如果 `ping` 通，说明雷达改 IP 成功。

---

## 6. 第三步：通过交换机统一接入

当摄像头和雷达都已经改到统一网段后，开始正式组网。

连接方式如下：

```text
海康摄像头 ─┐
            ├─ 交换机 ─ 电脑 eno1
M300 雷达  ─┘
```

电脑网口固定配置为：

- IP：`192.168.10.10`
- 子网掩码：`255.255.255.0`

命令如下：

```bash
sudo ip addr flush dev eno1
sudo ip addr add 192.168.10.10/24 dev eno1
sudo ip link set eno1 up
```

---

## 7. 第四步：统一扫描和验证

两个设备都接入交换机后，统一扫描：

```bash
sudo arp-scan --interface=eno1 192.168.10.0/24
```

正常情况下应能看到类似：

```text
192.168.10.101  xx:xx:xx:xx:xx:xx
192.168.10.201  xx:xx:xx:xx:xx:xx
```

再分别验证：

```bash
ping -c 4 192.168.10.101
ping -c 4 192.168.10.201
```

如果都正常，说明统一组网成功。

---

## 8. 摄像头抓流验证

海康摄像头改到统一网段后，RTSP 地址也要改成新地址：

```text
rtsp://用户名:密码@192.168.10.101:554/Streaming/Channels/101
```

预览命令：

```bash
ffplay -rtsp_transport tcp "rtsp://用户名:密码@192.168.10.101:554/Streaming/Channels/101"
```

录像命令：

```bash
ffmpeg -rtsp_transport tcp -i "rtsp://用户名:密码@192.168.10.101:554/Streaming/Channels/101" -t 30 -c copy test.mp4
```

---

## 9. 推荐的最终 IP 记录表

建议后续就固定使用下面这组地址：

| 设备 | IP | 备注 |
|---|---|---|
| 电脑 `eno1` | `192.168.10.10` | 统一采集入口 |
| 海康摄像头 | `192.168.10.101` | 视频流采集 |
| M300 雷达 | `192.168.10.201` | 雷达数据采集 |

---

## 10. 常见问题

### 1. 为什么不建议一开始就全部接交换机一起改？

因为：

- 初始网段不同
- 改 IP 时容易混淆
- 若设备默认地址重复会冲突
- 一台一台改，最容易确认修改结果

### 2. 为什么改完 IP 后突然访问不到？

因为设备切换到了新地址，旧地址立即失效。你必须把电脑网口也切换到新网段，才能继续访问。

### 3. 网关必须填吗？

如果只是：

- 电脑
- 交换机
- 摄像头
- 雷达

组成一个本地采集网络，网关可以先不填。

如果后续接入路由器或需要跨网段通信，可以再补网关。

### 4. 如果雷达不知道怎么改 IP 怎么办？

优先查该型号的厂家文档，确认它的配置方式是：

- Web 页面
- 专用配置软件
- TCP 命令
- UDP 命令
- 串口参数命令

不同雷达改 IP 的方式差别会很大。

---

## 11. 最短执行清单

如果你只想按最短步骤做，可以照下面执行：

1. 单独接海康摄像头
2. 电脑设为 `192.168.1.10/24`
3. 登录 `192.168.1.64`，改成 `192.168.10.101`
4. 单独接 M300 雷达
5. 电脑设为 `192.168.158.15/24`
6. 用雷达对应方式把 `192.168.158.98` 改成 `192.168.10.201`
7. 电脑设为 `192.168.10.10/24`
8. 摄像头和雷达一起接交换机
9. 分别 `ping 192.168.10.101` 和 `ping 192.168.10.201`
10. 对摄像头继续做 RTSP 拉流验证

---

## 12. 建议

- 完成改 IP 后，及时把最终地址记录到设备清单
- 给网线和设备贴标签，避免后续接错
- 摄像头、雷达、电脑都尽量使用固定静态 IP
- 如果设备越来越多，建议统一采用编号命名和地址规划
